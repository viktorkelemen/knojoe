- unless local_assigns.has_key? :check_role
  - check_role = true

%p{class: ('myview' if check_role && current_user == message.author)}
  = message.content
  %br
  = message.created_at.strftime("%Y/%m/%d %H:%m")