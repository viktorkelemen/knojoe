%h2 Chat
= render 'timer'

#messages{ 'data-id' => @chat.id }= render @messages

= render 'message_form'

:javascript
  jQuery(function($) {
    $('#messages').scrollTop($('#messages').prop("scrollHeight"));
  });