Your conversation on <%= @chat.created_at %>.

Chat log
========

<% @chat.messages.each do |message| %>
<%= message.author == @user ? 'Me: ' : 'Responder: ' %><%= message.content %>
<% end %>

Check on the web:
<%= review_chat_url(@chat.id) %>
